# ==============================================================================
# 🚀 RENTAL HUB — FRONTEND CLIENT ENVIRONMENT TEMPLATE
# ==============================================================================
# Copy this file to '.env.local' inside the frontend folder before running Next.js:
#   $ cp env.example .env.local
# ==============================================================================

# Public API Gateway
# Points to the running instance of the Express API backend.
# Default development port is 5000.
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
